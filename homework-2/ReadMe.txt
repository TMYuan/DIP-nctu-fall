The result is in "data" directory.

environment:
Ubuntu 17.04
Python 3.6

module dependency:
numpy, matplotlib, pillow


1. Open jupyter notebook (or use %pip3 install jupyter to install it)
2. Open DIP-hw-2.ipynb(white balance) or DIP-hw-2-2(color transformation)
3. Kernel-> Restart & Run All
4. See result in cell or output image in "data" directory

Or watch the code in html format.

For white balance task: output file is "wb.bmp"
For color transformation task: output file is "trans.jpeg"

"wb_test.bmp" is used in report.
